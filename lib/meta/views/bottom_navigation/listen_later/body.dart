import 'package:flutter/material.dart';

class ListenLaterView extends StatelessWidget {
  const ListenLaterView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('AddView'),
    );
  }
}
